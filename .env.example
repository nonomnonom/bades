# Semua env Docker ada di packages/bades-docker/
#
# Stack GHCR:
#   cp packages/bades-docker/.env.example .env
#   docker compose -f packages/bades-docker/docker-compose.yml up -d
#
# Native dev (yarn start) pakai packages/server/.env — lihat CLAUDE.md.
