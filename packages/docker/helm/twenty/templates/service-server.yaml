{{- if .Values.server.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bades.fullname" . }}-server
  namespace: {{ include "bades.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "bades.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.server.service.type | default "ClusterIP" }}
  selector:
    app.kubernetes.io/name: {{ include "bades.name" . }}
    app.kubernetes.io/component: server
  ports:
    - name: http-tcp
      port: {{ .Values.server.service.port }}
      targetPort: {{ include "bades.server.containerPort" . }}
      protocol: TCP
{{- end }}
