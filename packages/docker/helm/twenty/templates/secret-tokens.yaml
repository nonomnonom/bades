{{- if .Values.secrets.tokens.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.tokens.name | default "tokens" }}
  namespace: {{ include "bades.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ include "bades.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: tokens
stringData:
  accessToken: {{ include "bades.secret.tokens.access" . | quote }}
{{- end }}
