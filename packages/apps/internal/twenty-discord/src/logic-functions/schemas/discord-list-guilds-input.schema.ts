import { type InputJsonSchema } from 'sdk/logic-function';

export const discordListGuildsInputSchema: InputJsonSchema = {
  type: 'object',
  properties: {},
  required: [],
  additionalProperties: false,
};
